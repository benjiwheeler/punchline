
<%= form_for meme, method: :post, url: paths_soft_user_reset_path do |f| %>
  <%= hidden_field_tag :scope, :single %>
  <%= f.hidden_field :id %>
  <%= f.submit "Soft reset this user's votes\non this meme (use in dev)", class: "reset_button" %>
<% end %>

